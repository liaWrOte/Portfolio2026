import React from 'react';
import PropTypes from 'prop-types';

const Loader = () => {
  return (
    <span>
      Loading
    </span>
  );
};

Loader.propTypes = {
};

Loader.defaultProps = {
};

export default Loader;
